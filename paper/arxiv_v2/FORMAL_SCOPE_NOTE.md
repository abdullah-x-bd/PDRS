# Formal scope interpretation

The record-level theorem depends on injective lowering. The audit language enforces this through typed values, normalized strings, unique branch labels, and path-wise field uniqueness. A richer frontend must prove or test equivalent conditions. Token-level bijection alone does not establish record-level uniqueness when distinct paths can lower to equal records.
