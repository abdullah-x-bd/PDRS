# FinSpace package status

The branch contains a staged source archive that is decoded, tested, built, and committed by the FinSpace materialization workflow. This marker is removed or superseded once the real package tree is present.
