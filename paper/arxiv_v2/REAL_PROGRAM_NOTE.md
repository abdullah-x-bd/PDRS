# Real-program claim boundary

The first finance evidence layer establishes integration, reproducible object identity, and expected conformance behavior. The extension increases boundary and mutation coverage. Neither layer currently establishes a previously unknown defect. Parser acceptance, validator disagreement, or numerical deviation remains an observation until a target maintainer or independent expert confirms a defect against the target's intended semantics.
