# Statistical interpretation

The campaign-level bootstrap answers whether the median paired defect-count difference is supported under the declared benchmark. It does not convert a confidence interval that crosses zero into evidence of equality. It also does not pool correlated budget prefixes as independent rows. The paper reports method advantages only when the interval excludes zero and labels the remaining comparisons uncertain.
