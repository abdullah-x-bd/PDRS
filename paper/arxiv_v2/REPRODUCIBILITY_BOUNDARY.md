# Reproducibility boundary

The deterministic synthetic analysis is reproducible from the pinned environment and fixed seeds. Runtime microbenchmarks and external target behavior remain dependent on the recorded hardware, compilers, packages, XSDs, and execution date. The paper reports these dependencies instead of treating all output as byte-identical across platforms.
