# Residual external-baseline gaps

The revision completes the requested conceptual and implemented comparison programme with the existing native Feat evidence, a direct SciFe literature comparison, and an exact reduced-MDD baseline. Two further integrations would strengthen a later venue submission:

1. execute SciFe itself across the complete matched corpus;
2. execute an external d-DNNF or KUS implementation across constraint-equivalent domains.

These items require separate language/toolchain adapters and are recorded as follow-up evidence. The manuscript does not describe the reduced MDD as a full reproduction of either system, and it does not convert unsupported cells into PDRS wins.
