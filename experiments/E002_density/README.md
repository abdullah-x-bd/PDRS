# E002: Encoding density

Compare the minimum whole-domain fixed bit length with a transparent naive field allocation on fixed and dependent schemas.

This is not yet a comparison with production serializers. It is a deterministic lower-level baseline intended to validate the measurement pipeline and identify promising schemas for later ASN.1 PER and Protocol Buffers experiments.
